-- Vakk | Lazz Hub Lite (Fixed)
repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")

local LP = Players.LocalPlayer

local NS, CS = 60, 30
local LAGGER_SPEED = 15
local LAGGER_CARRY_SPEED = 24.5

local speedMode = false
local laggerToggled = false
local autoBatEnabled = false
local antiRagdollEnabled = false
local infJumpEnabled = false
local autoLeftEnabled = false
local autoRightEnabled = false
local autoTPEnabled = false

-- Simple Lite GUI
local gui = Instance.new("ScreenGui")
gui.Name = "LazzHubLite"
gui.ResetOnSpawn = false
gui.Parent = LP:WaitForChild("PlayerGui")

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 360, 0, 480)
main.Position = UDim2.new(0.5, -180, 0.5, -240)
main.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
main.BorderSizePixel = 0
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)

local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1, 0, 0, 50)
title.BackgroundTransparency = 1
title.Text = "Vakk | Lazz Hub Lite"
title.TextColor3 = Color3.fromRGB(255, 0, 0)
title.Font = Enum.Font.GothamBold
title.TextSize = 18

local function mkBtn(txt, y, callback)
    local btn = Instance.new("TextButton", main)
    btn.Size = UDim2.new(0.9, 0, 0, 48)
    btn.Position = UDim2.new(0.05, 0, 0, y)
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    btn.Text = txt
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 14
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
    
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- Buttons (Fixed callbacks)
mkBtn("Speed Mode: Normal", 60, function()
    speedMode = not speedMode
    -- Update button text properly
    for _, child in ipairs(main:GetChildren()) do
        if child:IsA("TextButton") and child.Text:find("Speed Mode") then
            child.Text = speedMode and "Speed Mode: Carry" or "Speed Mode: Normal"
            break
        end
    end
end)

mkBtn("Lagger Mode: OFF", 120, function()
    laggerToggled = not laggerToggled
    for _, child in ipairs(main:GetChildren()) do
        if child:IsA("TextButton") and child.Text:find("Lagger Mode") then
            child.Text = laggerToggled and "Lagger Mode: ON" or "Lagger Mode: OFF"
            break
        end
    end
end)

mkBtn("Auto Bat: OFF", 180, function()
    autoBatEnabled = not autoBatEnabled
    for _, child in ipairs(main:GetChildren()) do
        if child:IsA("TextButton") and child.Text:find("Auto Bat") then
            child.Text = autoBatEnabled and "Auto Bat: ON" or "Auto Bat: OFF"
            break
        end
    end
end)

mkBtn("Auto Left: OFF", 240, function()
    autoLeftEnabled = not autoLeftEnabled
    for _, child in ipairs(main:GetChildren()) do
        if child:IsA("TextButton") and child.Text:find("Auto Left") then
            child.Text = autoLeftEnabled and "Auto Left: ON" or "Auto Left: OFF"
            break
        end
    end
end)

mkBtn("Auto Right: OFF", 300, function()
    autoRightEnabled = not autoRightEnabled
    for _, child in ipairs(main:GetChildren()) do
        if child:IsA("TextButton") and child.Text:find("Auto Right") then
            child.Text
